.nexmoe-container {
    max-width: 800px;
    margin: auto;
    padding: 0 20px;
}

#nexmoe-content {
    width: 100%;
    padding: var(--gutter);
    margin: 0;
    background: var(--color-card);
    box-shadow: 0 0 1rem var(--color-shadow)
}